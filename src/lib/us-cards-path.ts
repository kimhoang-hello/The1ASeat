/**
 * Đường dẫn gốc của mục Thẻ Mỹ, trong file riêng vì header là Client Component
 * nằm trong layout gốc: import từ `us-credit-cards.ts` là kéo cả bộ dữ liệu
 * thẻ Mỹ vào bundle của mọi trang. Cùng lý do với `bank-compare-path.ts`.
 */
export const US_CARDS_BASE = "/us-credit-cards";
