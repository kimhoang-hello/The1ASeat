<!-- run at: Sun Aug 23 15:13:25 UTC 2026 -->

# Báo cáo sức khoẻ SEO — Ghế 1A — 23/08/2026

## 1. Tóm tắt

Site khoẻ mạnh, không có lỗi kỹ thuật nghiêm trọng nào tuần này. Đã crawl
toàn bộ 103 URL trong sitemap (tải mỗi URL 2 lần — bản thường và bản có
tham số ngẫu nhiên để phát hiện cache cũ), kiểm tra dữ liệu có cấu trúc
trên 8 trang mẫu, và soát cả 34 bài blog để tìm bài còn thiếu tiêu đề tìm
kiếm tiếng Việt. Redirect `www` → `ghe1a.com` (cả trang chủ lẫn `/blog`)
vẫn hoạt động đúng, không trang nào lỗi hay bị chặn index, canonical đúng
trên toàn bộ 103 trang, không phát hiện CDN của Hostinger phục vụ nội
dung cũ, dữ liệu có cấu trúc hợp lệ 100%, và không có bài nào thiếu tiêu
đề tìm kiếm tiếng Việt. Toàn bộ 16 trang trong mã nguồn đều đi qua
`pageMetadata()` và đều có `revalidate` — không tìm thấy lỗi mã nguồn nào
cần tự sửa tuần này.

**Thay đổi lớn nhất tuần này:** trang **`/bank-accounts`** (tài khoản
ngân hàng) đã được **công bố chính thức** — tăng từ 65 lên **103 URL**
trong sitemap, chủ yếu do 30 URL mới của mục này (1 trang danh sách + 29
tài khoản). Đã kiểm tra: toàn bộ các trang này trả 200, có canonical
đúng, không bị `noindex`, và dữ liệu có cấu trúc hợp lệ — không có vấn đề
kỹ thuật nào phát sinh từ việc công bố.

Có 2 điểm nhỏ đáng chú ý, không khẩn cấp — xem mục 2.

## 2. Cần bạn xử lý

**Không có việc gì khẩn cấp cần bạn làm tay tuần này.** Hai ghi chú dưới
đây là việc nội dung nhỏ, làm khi rảnh:

1. **Hai trang thẻ Marriott Bonvoy dùng chung một mô tả tìm kiếm y hệt
   nhau.** `/credit-cards/amex-marriott-bonvoy` (thẻ cá nhân) và
   `/credit-cards/amex-marriott-bonvoy-business` (thẻ doanh nghiệp) đều
   hiện dòng mô tả "Welcome bonus lên đến 110,000 điểm Bonvoy® khi mở thẻ
   mới." trên kết quả tìm kiếm. Tiêu đề hai trang khác nhau (có chữ
   "Business") nhưng mô tả thì giống hệt — Google có thể khó phân biệt
   hai trang này khi hiển thị. Sửa: vào Contentful, mở entry thẻ nào phù
   hợp (thường là thẻ business), sửa lại câu mô tả cho khác — ví dụ nêu
   rõ đây là thẻ dành cho doanh nghiệp/chủ tiệm, hoặc nêu một quyền lợi
   khác của bản business.

2. **PR #1 từ tuần trước (`seo-fix/2026-08-16`) đã lỗi thời, có thể đóng.**
   PR đó thêm dòng `revalidate = 60` vào `/bank-accounts`. Kiểm tra mã
   nguồn hiện tại (`main`) cho thấy dòng đó **đã có sẵn trên `main`** rồi
   (chắc bạn hoặc ai đó đã tự thêm tay), và `main` còn có thêm phần tương
   tự cho trang chi tiết từng tài khoản mà PR #1 chưa kịp che. PR #1 giờ
   không còn thay đổi gì thêm — có thể vào GitHub bấm "Close pull
   request" cho gọn. Không có rủi ro gì nếu để nguyên, chỉ là dọn dẹp.

Ghi chú phong cách (không phải lỗi, không cần xử lý gấp): 14 trang có mô
tả tìm kiếm ngắn hơn 70 ký tự — danh sách đầy đủ ở mục 4.

## 3. Đã sửa tự động

Không có. Bước kiểm tra mã nguồn (mục 5) không phát hiện lỗi nào tuần
này — không cần mở Pull Request mới.

## 4. Chi tiết kiểm tra

### Bước 2 — Crawl audit (sitemap, robots.txt, feed.xml, 103 URL)

| Hạng mục | Kết quả |
|---|---|
| `robots.txt`, `sitemap.xml`, `feed.xml` tải được, đúng định dạng | ✅ Cả 3 đều 200 |
| Mã trạng thái HTTP của 103/103 URL (tải 2 lần/URL) | ✅ Toàn bộ 200 |
| Thẻ canonical trên từng trang, trỏ đúng về chính URL đó | ✅ 103/103 |
| `<meta name="robots">` chứa `noindex` | ✅ Không trang nào bị |
| Mô tả tìm kiếm dài 70–175 ký tự | ⚠️ 14/103 URL ngắn hơn 70 ký tự (danh sách bên dưới) — không có URL nào dài quá 175 |
| Trùng `<title>` giữa các trang | ✅ Không trùng trang nào |
| Trùng mô tả tìm kiếm giữa các trang | ⚠️ 1 cặp trùng — xem mục 2, ý 1 |
| CDN phục vụ nội dung cũ (so bản thường với bản `?cb=` ngẫu nhiên) | ✅ Không phát hiện — 0/103 trang khác nhau, không trang nào bị Hostinger cache (`x-hcdn-cache-status` đều là `DYNAMIC`, không có `HIT` nào tuần này) |
| `www.ghe1a.com` và `www.ghe1a.com/blog` redirect về `ghe1a.com` | ✅ Cả hai đều mã 308, đúng đích |
| Sitemap sanity: URL trùng, sai tên miền, `lastmod` tương lai | ✅ Không có URL trùng, tất cả đúng tên miền `ghe1a.com`, không có `lastmod` ở tương lai |
| Tổng số URL trong sitemap | **103** (11 trang tĩnh + 5 trang chuyên mục + 34 bài blog + 23 thẻ tín dụng + 30 tài khoản ngân hàng) |

**Ghi chú kỹ thuật nhỏ (không phải lỗi, không cần bạn làm gì):** lần tải
`sitemap.xml` đầu tiên chỉ thấy 102 URL, thiếu đúng 1 bài mới
(`know-your-minimum-deal-nao-dang-de-toi-uu`); tải lại vài giây sau thấy
đủ 103. Đây là cách hoạt động bình thường của Next.js (bộ nhớ đệm nội bộ
"ISR" cứ 60 giây làm mới một lần), khác với sự cố CDN Hostinger từng gặp
trước đây — dấu hiệu phân biệt là header `x-hcdn-cache-status` vẫn báo
`DYNAMIC` (nghĩa là CDN không hề giữ bản cũ), và trang tự làm mới xong
trong vài giây mà không cần ai bấm "Clear cache". Đã kiểm tra lại bài
viết đó kỹ như mọi bài khác — không có vấn đề gì (200, canonical đúng,
đã có chữ tiếng Việt trong tiêu đề).

**14 URL có mô tả tìm kiếm ngắn hơn 70 ký tự** (không phải lỗi, chỉ phí
chỗ hiển thị trên Google — sửa khi rảnh):

| URL | Số ký tự |
|---|---|
| `/blog/ac-hotel-tokyo-ginza-review` | 53 |
| `/blog/element-amsterdam-review` | 55 |
| `/blog/andaz-seoul-gangnam-review` | 56 |
| `/blog/eva-air-16-hour-business-class-review` | 59 |
| `/credit-cards/amex-marriott-bonvoy` | 58 |
| `/credit-cards/amex-marriott-bonvoy-business` | 58 |
| `/credit-cards/amex-aeroplan-reserve` | 60 |
| `/blog/qatar-airways-a380-first-class-review` | 58 |
| `/blog/qatar-airways-qsuite-review` | 64 |
| `/blog/park-hyatt-tokyo-king-suite-review` | 62 |
| `/blog/hyatt-regency-paris-etoile-review` | 62 |
| `/blog/moxy-frankfurt-airport-hotel-review` | 63 |
| `/blog/grand-hyatt-seoul-review` | 66 |
| `/blog/moxy-frankfurt-city-center-review` | 67 |

### Bước 3 — Dữ liệu có cấu trúc (JSON-LD)

Kiểm tra trên: trang chủ, `/blog`, `/credit-cards`, 1 bài video
(`jal-a350-business-class-review`), 1 bài chữ
(`bat-dau-choi-miles-points-dieu-co-ban-nhat`), 1 trang chuyên mục
(`/blog/chuyen-muc/danh-gia`), 1 trang thẻ tín dụng
(`amex-aeroplan-reserve`), `/about`. Ngoài ra tranh thủ soát nhanh 2 trang
mới thuộc mục tài khoản ngân hàng (`/bank-accounts` và 1 trang chi tiết
tài khoản) vì đây là nội dung mới công bố tuần này — cả hai đều hợp lệ.

| Hạng mục | Kết quả |
|---|---|
| Toàn bộ JSON-LD là JSON hợp lệ | ✅ 8/8 trang mẫu (+ 2 trang tài khoản ngân hàng kiểm tra thêm) |
| Mọi trang có `Organization` + `WebSite` | ✅ |
| Bài viết có `BreadcrumbList` + `BlogPosting` | ✅ |
| Bài video có `VideoObject` lồng bên trong, đủ `uploadDate`, `thumbnailUrl`, `embedUrl` | ✅ |
| `/blog` và trang chuyên mục có `CollectionPage` với `mainEntity` là `ItemList` | ✅ (`/blog`: 34 mục, chuyên mục "đánh giá": 11 mục) |
| `/credit-cards` có `CollectionPage` với `ItemList` | ✅ (23 mục) |
| Trang thẻ tín dụng có `BreadcrumbList` + `CreditCard` kèm `offers` | ✅ |
| `/about` có `ProfilePage` với `Person` lồng trong `mainEntity` | ✅ |
| `dateModified` không sớm hơn `datePublished` | ✅ Kiểm tra cả 34/34 bài viết, không bài nào sai |

### Bước 4 — Bài thiếu tiêu đề tìm kiếm tiếng Việt (`seoTitleVi`)

Kiểm tra cả 34 bài `/blog/<slug>` (gồm cả bài mới
`know-your-minimum-deal-nao-dang-de-toi-uu`): so `<title>` (đã bỏ
` | Ghế 1A`) với `<h1>` của từng bài, tìm bài nào hai cái giống hệt nhau
**và** không có dấu tiếng Việt.

**Kết quả: không có bài nào.** Không cần thao tác gì ở Contentful cho
mục này tuần này.

### Bước 5 — Kiểm tra mã nguồn (repo)

| Hạng mục | Kết quả |
|---|---|
| Mọi `src/app/**/page.tsx` xuất `metadata`/`generateMetadata` qua `pageMetadata()` | ✅ 16/16 trang |
| Mọi `page.tsx` có `export const revalidate` | ✅ 16/16 trang |
| Link affiliate (`offer.applyUrl`) trong `src/app/credit-cards/` có `rel` chứa `sponsored` | ✅ Cả `/credit-cards` và `/credit-cards/[slug]` |
| Link ra ngân hàng trong `src/app/bank-accounts/` (nếu có hoa hồng) có `rel` chứa `sponsored` | ✅ Đã tự động: có hoa hồng thì gắn `sponsored`, không có hoa hồng (link thẳng về ngân hàng) thì gắn `nofollow` thường — đúng theo quy tắc "chỉ đánh dấu link có nhận tiền" |
| `src/app/sitemap.ts` vẫn khai báo route chuyên mục blog | ✅ Có (5 chuyên mục) |

16 trang đã kiểm tra: `/`, `/about`, `/award-flight-finder`,
`/bank-accounts`, `/bank-accounts/[slug]`, `/blog`, `/blog/[slug]`,
`/blog/chuyen-muc/[slug]`, `/calculator`, `/contact`, `/credit-cards`,
`/credit-cards/[slug]`, `/privacy`, `/terms`, `/transfer-bonuses`,
`/transfer-partners`. So với tuần trước, `/bank-accounts/[slug]` được
đếm là một khuôn trang riêng (trang chi tiết từng tài khoản) — trước đây
gộp chung.

## 5. So với tuần trước

- Tổng số URL trong sitemap: **103** (tuần trước: 65 — tăng 38, chủ yếu
  do công bố mục Tài khoản ngân hàng: +30 URL).
- Số bài blog: **34** (tuần trước: 30 — có 4 bài mới).
- Số trang thẻ tín dụng: **23** (tuần trước: 20 — có 3 thẻ mới).
- Số trang chuyên mục blog: **5** (tuần trước: 4 — có thêm chuyên mục
  "Tips").
- Số bài thiếu `seoTitleVi`: **0** (không đổi).
- Số mô tả tìm kiếm ngắn hơn 70 ký tự: **14** (tuần trước: 12 — ghi chú
  phong cách, không khẩn).
- Mô tả trùng lặp giữa 2 trang: **1 cặp mới phát hiện** (tuần trước: 0)
  — xem mục 2.
- Lỗi mã nguồn phát hiện tuần này: **0** (tuần trước: 1, đã có PR #1 —
  PR đó nay đã lỗi thời vì `main` tự có bản sửa riêng, xem mục 2, ý 2).
- CDN phục vụ nội dung cũ: **không phát hiện**, giống tuần trước.
