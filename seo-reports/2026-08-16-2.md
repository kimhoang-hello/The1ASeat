<!-- run at: Sun Aug 16 15:13:38 UTC 2026 -->

# Báo cáo sức khoẻ SEO — Ghế 1A — 16/08/2026

## 1. Tóm tắt

Site khoẻ mạnh, không có lỗi kỹ thuật nghiêm trọng nào. Đã crawl toàn bộ 65
URL trong sitemap (tải mỗi URL 2 lần), kiểm tra dữ liệu có cấu trúc trên 8
trang mẫu, và soát cả 30 bài blog để tìm bài còn thiếu tiêu đề tìm kiếm
tiếng Việt. Redirect `www` → `ghe1a.com` hoạt động đúng, không trang nào
lỗi hay bị chặn index, canonical đúng trên toàn bộ 65 trang, không phát
hiện CDN phục vụ nội dung cũ, dữ liệu có cấu trúc hợp lệ 100%, và không có
bài nào thiếu tiêu đề tìm kiếm tiếng Việt.

Đã tìm và **tự sửa 1 lỗi trong mã nguồn**: trang `/bank-accounts` (trang
mới, hiện đang được giấu khỏi Google bằng một "cờ tính năng" vì chưa công
bố) thiếu khai báo `revalidate` — nếu sau này bật công khai mà quên sửa,
CDN sẽ tự lưu cache 1 năm, đúng kiểu sự cố "nội dung cũ" từng xảy ra trên
site này trước đây. Đã mở Pull Request #1 để sửa (xem mục 3).

**Lưu ý về báo cáo:** thư mục `seo-reports/` sẵn có một file mang đúng tên
ngày hôm nay (`2026-08-16.md`), nhưng đó là **nội dung mẫu** được đưa vào
cùng lúc với toàn bộ mã nguồn ban đầu của site (không phải kết quả một lần
chạy thật trước đó) — nên báo cáo thật của tuần này được lưu với tên
`2026-08-16-2.md` để không ghi đè lên file đó.

## 2. Cần bạn xử lý

**Không có việc gì khẩn cấp cần bạn làm tay tuần này.**

Ghi chú nhỏ, không gấp, làm khi rảnh: 12 bài review khách sạn có đoạn "mô
tả tìm kiếm" (dòng chữ nhỏ hiện dưới tiêu đề xanh trên Google) hơi ngắn —
dưới 70 ký tự. Đây **không phải lỗi bị Google phạt**, chỉ là phí một chút
chỗ hiển thị. Danh sách đầy đủ ở mục 4. Muốn sửa thì vào Contentful → mở
bài → thêm 1 câu vào mô tả (ví dụ nêu số điểm cần đổi hoặc một điểm nổi
bật) → Publish.

## 3. Đã sửa tự động

**Pull Request #1** — https://github.com/kimhoang-hello/The1ASeat/pull/1

Thêm dòng `export const revalidate = 60;` còn thiếu vào trang
`/bank-accounts`. Giải thích ngắn gọn: `revalidate` là con số nói cho
Next.js biết "tối đa bao lâu thì được phép lấy lại nội dung mới từ
Contentful". Không khai báo thì Next.js mặc định 1 năm — và vì CDN của
Hostinger không tự xoá cache khi deploy, trang đó có thể kẹt với nội dung
cũ suốt một năm. Trang `/bank-accounts` hiện đang tắt index (chưa công bố)
nên chưa ảnh hưởng gì tới SEO thật, nhưng sửa trước khi công bố sẽ tránh
lặp lại đúng sự cố "cache cũ" từng xảy ra trên site.

Đã xác minh: `npm ci`, `npx tsc --noEmit`, `npx eslint src`, và
`npm run build` đều chạy sạch, không lỗi.

## 4. Chi tiết kiểm tra

### Bước 2 — Crawl audit (sitemap, robots.txt, feed.xml, 65 URL)

| Hạng mục | Kết quả |
|---|---|
| `robots.txt`, `sitemap.xml`, `feed.xml` tải được, đúng định dạng | ✅ Cả 3 đều 200, XML hợp lệ |
| Mã trạng thái HTTP của 65/65 URL (tải 2 lần/URL: bản thường + bản `?cb=`) | ✅ Toàn bộ 200 |
| Thẻ canonical trên từng trang, trỏ đúng về chính URL đó | ✅ 65/65 |
| `<meta name="robots">` chứa `noindex` | ✅ Không trang nào bị (kể cả `/bank-accounts` — đúng như thiết kế, vì trang này *nên* bị `noindex` lúc chưa công bố) |
| Mô tả tìm kiếm dài 70–175 ký tự | ⚠️ 12/65 URL ngắn hơn 70 ký tự (mục 2) — không có URL nào dài quá 175 |
| Trùng `<title>` hoặc mô tả giữa các trang | ✅ Không trùng trang nào |
| CDN phục vụ nội dung cũ (so bản thường với bản `?cb=` ngẫu nhiên) | ✅ Không phát hiện — xem ghi chú bên dưới |
| `www.ghe1a.com` và `www.ghe1a.com/blog` redirect về `ghe1a.com` | ✅ Cả hai đều mã 308, đúng đích |
| Sitemap sanity: URL trùng, sai tên miền, `lastmod` tương lai | ✅ Không có URL trùng, tất cả đúng tên miền `ghe1a.com`, không có `lastmod` ở tương lai |
| Tổng số URL trong sitemap | **65** (11 trang tĩnh + 4 trang chuyên mục + 30 bài blog + 20 thẻ tín dụng) |

**Ghi chú về kiểm tra CDN:** so sánh tiêu đề, mô tả, canonical và tiêu đề
lớn (`h1`) giữa bản tải thường và bản tải kèm `?cb=` ngẫu nhiên trên cả 65
trang — không trang nào khác nhau. Chỉ có 1 trang (`/terms`) được CDN xác
nhận đang lưu cache ở biên (`x-hcdn-cache-status: HIT`), tuổi cache
(`age`) mới 84 giây — còn rất xa mốc 1 năm từng gây sự cố trước đây.

### Bước 3 — Dữ liệu có cấu trúc (JSON-LD)

Kiểm tra trên: trang chủ, `/blog`, `/credit-cards`, 1 bài video
(`jal-a350-business-class-review`), 1 bài chữ
(`bat-dau-choi-miles-points-dieu-co-ban-nhat`), 1 trang chuyên mục
(`/blog/chuyen-muc/danh-gia`), 1 trang thẻ tín dụng
(`amex-aeroplan-reserve`), `/about`.

| Hạng mục | Kết quả |
|---|---|
| Toàn bộ JSON-LD là JSON hợp lệ | ✅ 8/8 trang |
| Mọi trang có `Organization` + `WebSite` | ✅ |
| Bài viết có `BreadcrumbList` + `BlogPosting` | ✅ |
| Bài video có `VideoObject` lồng bên trong, đủ `uploadDate`, `thumbnailUrl`, `embedUrl` | ✅ |
| `/blog` và trang chuyên mục có `CollectionPage` với `mainEntity` là `ItemList` | ✅ (`/blog`: 30 mục, chuyên mục "đánh giá": 11 mục) |
| `/credit-cards` có `CollectionPage` với `ItemList` | ✅ (20 mục) |
| Trang thẻ tín dụng có `BreadcrumbList` + `CreditCard` kèm `offers` | ✅ |
| `/about` có `ProfilePage` với `Person` lồng trong `mainEntity` | ✅ |
| `dateModified` không sớm hơn `datePublished` | ✅ Kiểm tra cả 30/30 bài viết, không bài nào sai |

### Bước 4 — Bài thiếu tiêu đề tìm kiếm tiếng Việt (`seoTitleVi`)

Kiểm tra cả 30 bài `/blog/<slug>`: so `<title>` (đã bỏ ` | Ghế 1A`) với
`<h1>` của từng bài, tìm bài nào hai cái giống hệt nhau **và** không có
dấu tiếng Việt.

**Kết quả: không có bài nào.** Không cần thao tác gì ở Contentful cho mục
này tuần này.

### Bước 5 — Kiểm tra mã nguồn (repo)

| Hạng mục | Kết quả |
|---|---|
| Mọi `src/app/**/page.tsx` xuất `metadata`/`generateMetadata` qua `pageMetadata()` | ✅ 15/15 trang |
| Mọi `page.tsx` có `export const revalidate` | ⚠️ 14/15 — thiếu ở `/bank-accounts` → **đã sửa, xem mục 3** |
| Link affiliate (`offer.applyUrl`) trong `src/app/credit-cards/` có `rel` chứa `sponsored` | ✅ Cả `/credit-cards` và `/credit-cards/[slug]` |
| `src/app/sitemap.ts` vẫn khai báo route chuyên mục blog | ✅ Có (4 chuyên mục) |

15 trang đã kiểm tra: `/`, `/about`, `/award-flight-finder`,
`/bank-accounts`, `/blog`, `/blog/[slug]`, `/blog/chuyen-muc/[slug]`,
`/calculator`, `/contact`, `/credit-cards`, `/credit-cards/[slug]`,
`/privacy`, `/terms`, `/transfer-bonuses`, `/transfer-partners`. Hai trang
mới so với lần kiểm tra gần nhất còn dấu vết trong repo là
`/award-flight-finder` và `/bank-accounts` (trang sau đang chưa công bố).

## 5. So với tuần trước

Không có báo cáo thật nào của "tuần trước" để so — file
`seo-reports/2026-08-09.md` có sẵn trong repo cũng là dữ liệu ví dụ đi
kèm mã nguồn ban đầu (báo cáo đó tự mô tả là "không crawl được vì bị chặn
mạng"), không phải kết quả của một lần chạy thật. Coi báo cáo này là **mốc
nền đầu tiên thực sự đáng tin** cho các số liệu dưới đây — từ tuần sau sẽ
so trực tiếp với các con số này.

- Tổng số URL trong sitemap: **65** (11 tĩnh + 4 chuyên mục + 30 bài blog + 20 thẻ tín dụng).
- Số bài thiếu `seoTitleVi`: **0**.
- Số mô tả tìm kiếm ngắn hơn 70 ký tự: **12** (ghi chú phong cách, không khẩn — mục 2).
- Lỗi mã nguồn phát hiện và tự sửa: **1** (`/bank-accounts` thiếu `revalidate`, PR #1).
