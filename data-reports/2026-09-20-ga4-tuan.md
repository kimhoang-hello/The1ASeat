# Ghế 1A — Insight GA4 tuần 13/09 → 19/09/2026

Nguồn: GA4 property `a403384074p548450267` (tag `G-5EJS75L7SK`), đọc qua Chrome
ngày 20/09/2026 (chạy tự động sáng Chủ nhật). Kỳ so sánh: **06/09 → 12/09**, đọc
LẠI hôm nay nên đã settle (một vài số khác báo cáo 13/09: sessions 312 → 313,
views ~1,181 → 1,200, key events 74 → 73, Organic Social 170 → 175, Unassigned
62 → 41).

**Đã qua hai vòng phản biện với Codex (`gpt-5.6-sol`, effort high)** — xem mục 9. Bản vá code newsletter ở mục 6 được commit cùng báo cáo này; **chưa chắc đã deploy** khi bạn đọc.

> **Cảnh báo đọc số:** ngày **19/09 (hôm qua) chưa settle**, và nó chứa TOÀN BỘ
> phần bất thường của tuần này (mục 1). Mọi số "cả tuần" bên dưới đều kèm số
> **13–18/09 (6 ngày cộng khớp, không phải "đã settle": 18/09 vẫn có thể còn trong cửa sổ 24–48h)** để đối chiếu.

## 1. Bất thường tuần này nằm gọn trong ngày 19/09

| | 13–19/09 (7 ngày) | 13–18/09 (6 ngày) | 19/09 riêng |
|---|---|---|---|
| Phiên (tổng) | 360 | 277 | 83 |
| Tổng các dòng kênh | 397 (thừa 37) | 277 (khớp) | — |
| Tổng các dòng nguồn/medium | 400 (thừa 40) | 277 (khớp) | 123 (thừa 40) |

- Bảng nguồn/medium của **riêng 19/09** cộng ra 123 trong khi tổng là 83 (thừa
  40) — đúng bằng phần thừa của cả tuần. 6 ngày trước đó cộng khớp tuyệt đối.
- Ba dòng "lạ" của tuần: `(not set)` **46** phiên, `(data not available)` **30**
  phiên (= kênh `Cross-network` 30 = landing page rỗng 30), đều xuất hiện
  **NGUYÊN SỐ ĐÓ ở riêng ngày 19/09**. Hai dòng đầu có 0–3 engaged session mà vẫn
  có engagement time trung bình 1m31s / 42s — **mâu thuẫn nội bộ với
  định nghĩa GA4** (`(not set)` có 3 key event mà 0 engaged session — key event
  một mình đã buộc phiên đó engaged). Codex vòng 1 cãi điểm này, vòng 2 công nhận
  mình đúng.
- Kết luận đúng mức: phần **THỪA** (40 dòng) và các dòng `(not set)` 46 /
  `(data not available)` 30 / Cross-network 30 chỉ xuất hiện ở ngày 19/09 chưa
  settle — **đừng trích chúng**. Nhưng KHÔNG được nói cả Unassigned 77 là hiện
  vật: 6 ngày cộng khớp vẫn có Unassigned **26** (= `catch-the-points/game` 23 + `hero`
  2 + `verify_hero_param` 1, đúng khít), là phần thật. Chỉ phần tăng thêm của 19/09
  (~51) nghi chưa settle. Đọc lại 19/09 sau ~22/09 để đóng.

## 2. Số tổng (Home report, đã zoom xác nhận mũi tên)

| Chỉ số | 13–19/09 | 06–12/09 (settled) | Đổi |
|---|---|---|---|
| Người dùng hoạt động | 235 | — | ↑ +11.9% |
| Phiên | 360 | 313 | ↑ +15.0% |
| Lượt xem | 1,529 | 1,200 | ↑ +27.4% |
| Key events | 58 | 73 | ↓ −20.5% |

Mũi tên: Sessions/Views/Active users xanh ↑, Key events đỏ ↓ (đã zoom ảnh gốc,
không suy từ text). Kiểm chéo: 360/313 = +15.0%; 1,529/1,200 = +27.4%; 58/73 =
−20.5% — khớp.

**Nhưng tăng phiên/view bị ngày 19/09 kéo:** 19/09 riêng có 83 phiên và 513 lượt
xem (410 trong đó là `/credit-cards/goi-y`, xem mục 4). Bỏ 19/09: 277 phiên /
1,016 lượt xem trong 6 ngày. So 6 ngày với 7 ngày của tuần trước không táo với
táo, nên **chưa kết luận được tuần này tăng thật hay không**.

Key events giảm vì hai thứ (đều là số có tên): `apply_clicked` 58 → 52 và
`newsletter_subscribed` 15 → 6 (trong đó 2 là event thử của mình ngày 13/09 —
mục 6, nên thật là 4).

## 3. Kênh và nguồn

Chỉ dùng số **13–18/09** (277 phiên, cộng khớp) cho phần cơ cấu:

| Kênh | 13–18/09 | 06–12/09 |
|---|---|---|
| Organic Social | 127 | 175 |
| Direct | 80 | 72 |
| Organic Search | **43** | 25 |
| Unassigned | 26 | 41 |
| Referral | 1 | 0 |

- **Google organic có session key event rate cao nhất trong các nguồn ≥ 30 phiên, và đang lên:** 40 phiên / 35 engaged
  (87.5%) / **27 key events** (51% mọi key event của 13–18/09, session key event
  rate 27.5%) / engagement time 2m56s. Tuần trước cả 7 ngày: 24 phiên. Mẫu nhỏ
  (40 phiên) — đọc là "hướng tốt", không phải xu hướng chắc. "Cao nhất" chỉ theo
  session key event rate trong các nguồn ≥ 30 phiên của 13–18/09: google 27.5% vs
  Direct 10%, facebook.com 10.4%, l.facebook.com 9.4%. Key event đếm theo event,
  một phiên có thể góp nhiều. 40 phiên/6 ngày vs 24/7 ngày là so khác độ dài, nhưng
  chiều hướng vẫn tăng theo đầu ngày (6.7 vs 3.4 phiên/ngày).
- Facebook (facebook.com + l. + lm. + m.) 127 phiên trong 6 ngày vs 175 phiên tuần
  trước — tuần trước có đỉnh 11/09 (110 phiên landing `/`, 89 từ Organic Social),
  tuần này không có đỉnh tương đương.
- **`catch-the-points / game` = 2 người dùng duy nhất, 0 người mới**: 31 phiên
  tuần này (39 tuần trước, cũng đúng 2 người). Landing của chúng rải khắp nơi (`/`
  14, `/credit-cards/goi-y` 11, blog…), 0 key event tuần này (8 key event tuần
  trước). Nghĩa là con số 31/39 phiên "từ game" KHÔNG phải luồng người chơi — là
  hai trình duyệt quay lại nhiều lần với nguồn gắn `catch-the-points/game`.
  **Chưa biết vì sao** hai trình duyệt đó giữ nguồn này dù UTM đã bỏ từ 06/09 (có
  thể là thiết bị của chính mình/người đọc thân — chưa xác nhận, GA4 không có
  bộ lọc internal traffic). "2 người dùng" là số GA4 user (cookie/thiết bị), chưa
  chắc là 2 người thật; và không chứng minh được chúng không liên quan game.
- `www5.cj.com / referral` 1 phiên: domain trùng mạng affiliate CJ. Một phiên, chưa
  đủ để suy hành trình nào — chỉ ghi nhận.

## 4. Trang, và trang gợi ý mới

Lượt xem 13–19/09 (6 ngày trước 19/09 trong ngoặc khi cần):

| Trang | Views | Users |
|---|---|---|
| `/credit-cards/goi-y` (recommender, **mới**) | 550 (**410 riêng 19/09**) | 59 |
| `/` | 202 | 105 |
| `/credit-cards` | 161 | 49 |
| `/blog/asia-miles-transfer-bonus-15-rbc-avion` | 94 | 61 |
| `/award-flight-finder` | 60 | 27 |

- `/credit-cards/goi-y`: 36% mọi lượt xem của tuần, **9.3 views/người**, 2m08s, chỉ
  **1 key event** (1 `apply_clicked`, placement `recommender_primary`). 74% lượt
  xem dồn vào ngày chưa settle (19/09: 410 view / 52 người / 56 phiên landing tại
  đó). 18/09: 80 view / 9 người; 13–17/09 cộng ~60. **Số view này rất có thể là
  view THEO TỪNG BƯỚC, không phải lượt đọc trang.** Đã kiểm ba mắt xích: (1) mọi
  thao tác của trang là Server Action rồi `redirect(PATH)`
  (`goi-y/actions.ts:106,141,147`), Next xử lý redirect cùng URL bằng
  `history.replaceState`; (2) Enhanced measurement của stream `Ghế 1A` **đang bật
  "Page changes based on browser history events"** (đọc trực tiếp ở GA4 Admin
  20/09, chỉ xem, không đổi gì); (3) `@next/third-parties` chỉ khởi tạo
  `gtag('config')`, page_view khi đổi history do Enhanced measurement phát. Vậy
  mỗi bước bảng câu hỏi có thể sinh một `page_view` — 9.3 view/người là dấu vết
  khớp. **Chưa được kiểm bằng DebugView/Network** (mình không được POST vào
  endpoint của chính app), nên là "rất có khả năng", chưa phải sự thật. Hệ quả nếu
  đúng: 36% lượt xem của site và "views/user" của `goi-y` bị thổi phồng, và tổng
  Views tuần này (+27.4%) một phần là do trang này. (Engagement time thì KHÔNG bị
  nhân đôi bởi page_view thêm — Codex vòng 2, mình kiểm lại đồng ý.)
  **Không đủ căn cứ nói người đọc thật dùng trang nhiều hay ít**: vừa chưa settle,
  vừa 11 phiên thuộc 2 user `catch-the-points/game` ở mục 3.
- `/` giảm view (289 → 202), phù hợp với việc tuần trước có đỉnh Facebook 11/09
  (chưa phân rã đủ để nói "vì").
- Path lạ trong bảng: `/admin/reco-debugger` (1 phiên landing), `/api/health` (2
  view). Chưa rõ nguồn (Codex: repo không có route `/api/health`) — không suy diễn.

## 5. `apply_clicked` — KHÔNG PHẢI doanh thu, là click ra ngoài

| Kỳ | Sự kiện / người |
|---|---|
| 06–12/09 (settled) | 58 / 28 |
| 13–19/09 | **52 / 27** |

Gần như đứng yên (−10% sự kiện, −4% người). **6 ngày cộng khớp 13–18/09: 47 sự kiện /
23 người** (19/09 chỉ góp 5) — nên phần này ít bị ngày chưa settle ảnh hưởng: bình
quân 7.8/ngày vs 8.3/ngày tuần trước. Bảng Placement 13–18 giống hệt 13–19 trừ
`card_list_image` 3 (thay vì 4), `card_detail` 1+1 (thay vì 4 tổng) và
`recommender_primary` 0 (nằm ở 19/09).

**Theo trang** (52, cộng đủ): `/credit-cards` **25 (48%)**; nhóm `tot-nhat`
(travel 4 + offers 2 + nguoi-moi 3) 9 (17%); `/` 3; `rbc-avion-visa-platinum` 3;
blog `top-3-cong-cu` 3; `so-sanh` 1; `goi-y` 1; 7 trang còn lại 1 mỗi trang (bảng
đầy đủ ở phụ lục cuối; 25+9+3+3+3+1+1+7 = 52).

**Theo vị trí (`Placement`, 52 cộng đủ):** `card_list` 21 + `card_list_image` 4 =
**25**; `best_cards_*` (kể cả `_image`) **9**; `card_detail` 4 + `card_detail_image` 3
= **7**; `post_body` 4; `home_offers` + `_image` 3; `account_list` 2;
`card_compare` 1; `recommender_primary` 1. `card_detail_rail` (9 click tuần
trước) = **0** tuần này (cả 6 ngày cộng khớp). Placement còn trong code
(`credit-cards/[slug]/page.tsx:147`) và chỉ hiện từ breakpoint `xl`; 0 event quan
sát được, **chưa phân biệt được "không ai bấm" với "hỏng"** — chưa có test runtime.

**Theo thẻ (`Product`, đã loại 3 dòng slug bài viết):** RBC® Avion® Visa Platinum
**10**, TD® First Class Travel® Visa Infinite* **7**, RBC® Avion® Visa Infinite 4,
rồi CIBC® Aventura® Visa Infinite*, Amex Green, Scotiabank® Scene+ Visa Student,
Amex Aeroplan®* Reserve, Amex Aeroplan®* Business Reserve **3 mỗi thẻ**, còn lại
1–2. **Bảng xếp hạng thẻ đổi hoàn toàn so với tuần trước (tạm thời — gồm 19/09)** (tuần trước dẫn đầu RBC
Avion VI 9, CIBC Aventura Gold 6, Scotia Gold Amex 6; tuần này Scotia Gold Amex =
0). Với ~50 click/tuần, **không đủ căn cứ xếp hạng thẻ theo một tuần** — chỉ nói
được RBC Avion Platinum dẫn đầu tuần này, và chương trình transfer bonus 15% RBC
đang được đẩy.

**Kiểm bản vá đếm đôi `CardSpotlight` (commit `730a859`, 13/09): đạt, N=1.**
Trang `asia-miles-transfer-bonus-15-rbc-avion` có đúng **1** `apply_clicked` với
`product=rbc-avion-visa-platinum` (slug thẻ), `placement=post_body`, và **không**
có event `product=<slug bài>` đi kèm — nếu tracker còn bắn thì phải ra 2. Một
điểm dữ liệu; cần thêm tuần. **Quy tắc kiểm cũ trong AGENTS.md ("tổng `post_body`
phải BẰNG tổng dòng Product dạng slug-bài") nay SAI:** sau bản vá, click vào khối
`CardSpotlight` hợp lệ mang `placement=post_body` + slug thẻ, nên tổng `post_body`
(4) > dòng slug bài (3) là bình thường. Quy tắc mới: trên mỗi trang blog, đếm
event theo Product — một click khối thẻ chỉ được ra một event slug thẻ.

## 6. Newsletter và lỗi `source` phát hiện tuần này

`newsletter_subscribed`: **6 sự kiện = 4 thật + 2 event thử của mình 13/09**
(`source` = `verify_hero_param_20260913`, tham số CŨ), tuần trước 15 (đều
`source=hero`). 4 thật đều `hero`, đều từ trang chủ; hai cửa kia (`page_cta`,
`start_here`) vẫn 0. Tuần trước 15 trùng với đỉnh Facebook 11/09 vào trang chủ.

**Phát hiện: tham số `source` trên event làm BẨN nguồn phiên GA4.** Bằng chứng có
đối chứng: event thử gửi `source=verify_hero_param_20260913` → trong bảng Session
source/medium xuất hiện dòng `verify_hero_param_20260913 / (not set)` (1 phiên, 2
event) — chuỗi đó không tồn tại ở đâu khác. Tương tự dòng `hero / (not set)` (2
phiên/tuần, 2 tuần liền, 0 engaged) do `NewsletterForm` gửi `source: "hero"`.
Đây là lời giải cho dòng `hero/` bí ẩn của báo cáo 13/09 (phép thử 13/09 chỉ kiểm
`tid`, đúng là không bị ghi đè — nhưng không nhìn bảng nguồn). **Cơ chế chưa rõ:**
Google nói nguồn mới giữa phiên không mở phiên mới, và 15 lượt đăng ký tuần trước
chỉ ra 2 phiên `hero`; mình chưa giải thích được vì sao MỘT SỐ event mở ra phiên
riêng mang nguồn đó. Nên đọc là "`source` lọt vào Session source", KHÔNG khẳng định
nó "đẻ phiên". Sáu ngày 13–18/09 cộng khớp 277 kể cả dòng `verify_…` nên nó không
phải nguồn của phần thừa ở mục 1.

**Đã vá code** (`newsletter-form.tsx`, phòng ngừa): tham số đổi `source` →
`newsletter_source`. Đổi tên là an toàn (Codex kiểm: cả 3 form dùng chung một
component, `newsletter_source` không thuộc danh sách tên bị cấm của GA4; không còn
event nào khác gửi `source/medium/campaign/term/content/tracking_id/page_location`).
**Cần bạn làm trong GA4 Admin → Custom definitions:** đăng ký custom dimension
mới (scope Event, tham số `newsletter_source`, tên "Newsletter source"); dimension
cũ (`source`) sẽ ngừng nhận dữ liệu và có thể lưu trữ. Đến khi đăng ký, các
lượt đăng ký mới vẫn được đếm bình thường nhưng KHÔNG tách được theo nguồn form.
Ngày 13/09 có 2 event thử lẫn trong tổng `newsletter_subscribed`.

## 7. Search Console

Hai báo cáo, hai phạm vi — đừng lẫn:

| | 13–19/09 (2 ngày cuối có thể thiếu) | 06–12/09 (settled) |
|---|---|---|
| **Tổng** (báo cáo *Google organic search traffic*): click / impression | **27 / 110** | **9 / 45** |
| Click có landing `/` (trang chủ, gần như chắc là truy vấn thương hiệu) | 25 | 9 |
| Click landing khác | 2 (`/blog` 1, bài Park Hyatt Kyoto 1) | 0 |
| **Chỉ phần có query hiển thị** (báo cáo *Queries*): click / impression | 16 / 47 | 8 / 16 |
| — trong đó query `ghe1a` | 15 click / 15 imp | 6 click / 10 imp |

- **Báo cáo Queries chỉ thấy một phần:** 11 click và 63 impression tuần này không
  lộ query (Google ẩn query hiếm vì riêng tư). Vì vậy KHÔNG được viết "click phi
  thương hiệu = 1" hay "impression phi thương hiệu = 32" (bản đầu của mình viết
  vậy, Codex vòng 2 bắt được). Đúng là: trong 16 click có query hiển thị, 15 là
  `ghe1a`; ở cấp tổng, 25/27 click vào trang chủ, 2/27 vào trang khác.
- Impression ngoài trang chủ (tổng − ~25 của `/`) ~85 vs ~30 tuần trước — nhiều
  gấp ~2.8, rải trên nhiều trang (About, award-flight-finder, blog, bank-accounts…);
  các query nhìn thấy thuộc cụm thẻ mới (Amex Green 9 impression, CIBC Privilege ~6,
  Park Hyatt Kyoto 5) với **vị trí trung bình phần lớn 49–94** (ngoại lệ `1a bàn
  cờ` vị trí 10): visibility rất thấp. Giữ kết luận hẹp: trang mới bắt đầu hiện,
  chưa xếp hạng. Đọc lại sau 22/09.
- GA4 Google organic **45 phiên vs GSC 27 click** (tuần trước 24 vs 9): không 1:1,
  chênh lệch chưa điều tra.

## 8. Địa lý, YouTube, game

- Quốc gia (active users): Canada 205 (+3.5%), Mỹ 13 (+550%), Việt Nam 6, Ba Lan 5,
  Pháp/Anh/Ấn Độ 1. Mỹ 13 và Ba Lan 5 từ nền rất nhỏ; **chưa biết** là người thật
  hay bot.
- **YouTube: vẫn 0 phiên** (không có dòng nào trong bảng nguồn). Chưa có số exposure của video nên số 0 không nói gì về hiệu quả video; van vẫn theo quyết định 04/09 (mở sau khi có bài thật đứng sau video).
- Mini-game: `game_started` **2**, `game_completed` 2, `post_game_recommendation_shown`
  2 (tuần trước 36 / 31 / 13). Game gần như im. Không phân biệt được "hết mới" với
  "site ngừng đẩy game" — cùng kết luận báo cáo 13/09.

## 9. Chắc chắn / chưa đủ căn cứ / chưa ngã ngũ

**Chắc chắn (đã đối chiếu ít nhất hai nguồn hoặc phép cộng khớp):**
- Ngày 19/09 chứa nguyên phần thừa 40 dòng của bảng nguồn và các dòng `(not set)`
  46 / `(data not available)` 30 / Cross-network 30; 13–18/09 cộng khớp 277.
- `catch-the-points / game` = 2 GA user quay lại (0 người mới) ở cả hai tuần: 31
  và 39 phiên. Không phải luồng người chơi.
- `apply_clicked` 52 sự kiện / 27 người (47 / 23 trong 13–18/09); phân rã theo
  trang, vị trí, thẻ đều cộng đủ 52; bản vá đếm đôi `CardSpotlight` đúng như mô tả
  (Codex kiểm code) và dữ liệu N=1 khớp bản vá.
- Enhanced measurement của stream đang bật "Page changes based on browser history
  events"; trang gợi ý redirect cùng URL bằng `replaceState`.
- Tổng Search Console: 27 click / 110 impression (13–19/09) vs 9 / 45; 25/27 click
  vào trang chủ.
- Google organic có session key event rate cao nhất trong các nguồn ≥30 phiên
  (27.5%) của 13–18/09.
- Chuỗi `verify_hero_param_20260913` xuất hiện trong Session source — tham số
  `source` của event lọt vào nguồn phiên.

**Chưa đủ căn cứ (đừng trích như sự thật):**
- 550 view của `/credit-cards/goi-y` là view theo từng bước: rất có khả năng,
  chưa xác nhận bằng DebugView/Network.
- Vì sao một số event `newsletter_subscribed` mở ra phiên `hero/(not set)` — cơ
  chế chưa rõ; chỉ biết `source` lọt vào Session source.
- Google organic "đang lên" (40 phiên/6 ngày vs 24/7 ngày); xếp hạng thẻ theo
  Product (~50 click, đổi hoàn toàn so với tuần trước); `card_detail_rail` = 0
  (không ai bấm hay hỏng — chưa có test runtime).
- Phần tăng của Unassigned ngày 19/09 (~51) và mọi số kênh/nguồn của riêng ngày đó.
- US 13 / Ba Lan 5 người dùng: người thật hay bot; `/api/health` 2 view (repo
  không có route này); danh tính 2 user `catch-the-points/game`.

**Mình với Codex:**
- **Codex đúng, đã sửa:** không loại được cả Unassigned 77 (6 ngày còn 26); "`source`
  đẻ phiên" quá mạnh; ghi sai tên tham số của event thử; tổng `apply_clicked` theo
  trang cộng ra 57 (sửa: 7 trang, cộng đủ 52); "toàn bộ click thương hiệu" sai;
  comment trong code còn khẳng định "đẻ phiên"; "6 ngày sạch" thành "6 ngày cộng
  khớp"; và — bắt lớn nhất vòng 2 — báo cáo bản đầu lấy báo cáo *Queries* làm tổng
  Search Console (16 click thấy được trong 27 click thật, 47 trong 110 impression).
  Mình đã đọc lại báo cáo *Google organic search traffic* để xác nhận 27/110 và 9/45
  trước khi sửa.
- **Mình đúng, Codex vòng 1 sai (vòng 2 công nhận):** "engagement time dương không
  mâu thuẫn với 0 engaged session" — `(not set)` có 3 key event nên không thể 0
  engaged session.
- **Codex vòng 2 đúng, mình bỏ:** "engagement time của `goi-y` bị page_view theo bước
  thổi phồng" — engagement time không nhân đôi theo page_view.
- **Chưa ngã ngũ / chưa kiểm lại:** vòng 2 của Codex kết bằng "còn chặn: CÓ" trên
  bản TRƯỚC khi mình sửa các điểm nó nêu (GSC, "chất lượng nhất", trạng thái commit,
  comment code, "6 ngày sạch"). Mình đã sửa hết nhưng KHÔNG chạy vòng 3 (vá đúng lỗi
  thật rồi dừng). Đọc mục 7 (Search Console) như bản đã sửa, chưa qua Codex lần nữa.

**Việc cần bạn quyết/làm:**
1. GA4 Admin → Custom definitions: đăng ký custom dimension mới `newsletter_source`
   (Event scope, tên "Newsletter source"). Đến khi làm, không tách được đăng ký theo
   form.
2. Muốn biết `goi-y` thật sự được đọc bao nhiêu: mở DebugView (hoặc Network
   `g/collect`) rồi đi hết bảng câu hỏi trên production; đếm số `page_view`. Nếu mỗi
   bước một view, cân nhắc tắt "Page changes based on browser history events" (ảnh
   hưởng cả site) hoặc chỉ đọc số `goi-y` bằng event riêng.
3. Không cần làm gì cho YouTube/game tuần này.


## Phụ lục — `apply_clicked` theo trang, 13–19/09 (52)

`/credit-cards` 25 · `/` 3 · `/credit-cards/rbc-avion-visa-platinum` 3 ·
`/blog/top-3-cong-cu-miles-and-points-2026` 3 · `/credit-cards/tot-nhat/travel` 4 ·
`/credit-cards/tot-nhat/nguoi-moi` 3 · `/credit-cards/tot-nhat/offers` 2 · 9 trang
1 click mỗi trang (`bank-accounts`, `bank-accounts/scotiabank-ultimate-package`,
`blog/asia-miles…`, `cibc-aventura-visa-infinite`, `goi-y`,
`rbc-avion-visa-infinite`, `rbc-avion-visa-infinite-privilege`, `so-sanh`,
`td-aeroplan-visa-infinite`).
