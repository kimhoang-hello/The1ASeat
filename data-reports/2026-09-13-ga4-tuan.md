# Ghế 1A — Insight GA4 tuần 06/09 → 12/09/2026

Nguồn: GA4 property `a403384074p548450267` (tag `G-5EJS75L7SK`), đọc trực tiếp
qua trình duyệt ngày 13/09/2026. Kỳ so sánh: 30/08 → 05/09 (đã settle hoàn
toàn, không còn cảnh báo "dữ liệu chưa xử lý xong").

**Đã qua ba vòng phản biện với Codex (`gpt-5.6-sol`, effort high) ngày
13/09/2026** — hai vòng chuẩn (chất vấn bản nháp, rồi bắt bác lại chính bản
vá) cộng một vòng kiểm hẹp cho hai chỗ tự sửa thêm sau đó. Xem mục 9.

> **Lưu ý:** ngày 11/09 và 12/09 vẫn trong cửa sổ 24–48 giờ GA4 có thể còn
> chỉnh số. Riêng 12/09 (hôm qua) đọc thận trọng nhất.

## 0. Tin tốt: hai việc sửa tuần trước ĐÃ CHỨNG MINH hoạt động

Báo cáo 06/09 để lại hai việc "chưa ngã ngũ" — tuần này có dữ liệu thật để
đóng cả hai.

**a) `tracking_id` → `recommendation_id` (commit `5d658b7`) đã sống lại đúng
event.** Tuần 30/08–05/09 `post_game_recommendation_shown`/`_clicked` bằng 0
tuyệt đối. Tuần này: `post_game_recommendation_shown` **12**,
`post_game_recommendation_clicked` **1**. Không nhiều (lượt chơi game giảm
mạnh tuần này — xem mục 3), nhưng con số khác 0 nghĩa là đường ống đo đã
thông.

**b) Cái mà tuần trước gọi "hàng Unassigned ghi 61 phiên, chưa rõ nguyên
nhân" — khi đọc LẠI đúng tuần đó (30/08–05/09) bằng dữ liệu đã settle, nó biến
mất.** Unassigned tuần đó giờ chỉ còn **7** phiên (không phải 61), tổng phiên
kênh cộng đúng khớp 331 = 331 (không còn thừa 47 dòng). Đúng như dự đoán thận
trọng nhất của báo cáo cũ: **dữ liệu chưa xử lý xong**, không phải mất nguồn
thật. **Đây là kết luận CHỈ áp dụng cho tuần 30/08–05/09** — tuần này
(06–12/09) lại có Unassigned=62 và thừa dòng của RIÊNG NÓ (mục 2), là một
lần bất thường MỚI, chưa được "đóng" theo cách tuần trước đã đóng. Bài học
giữ lại là phương pháp (đọc số trong 24–48 giờ đầu của một cửa sổ GA4 là đọc
số tạm), không phải một lần sửa xong vĩnh viễn.

## 1. Số tổng (theo Home report, đã có sẵn % tuần-trên-tuần)

**Sửa lỗi đọc mũi tên — bản nháp đầu ghi NHẦM dấu của Sessions và Views
thành tăng.** Đã zoom lại màn hình gốc để xác nhận màu/hướng mũi tên (không
chỉ đọc text, vì `get_page_text` không giữ được icon mũi tên): Sessions và
Views đều mũi tên ĐỎ, hướng XUỐNG. Bảng dưới đây là số ĐÚNG.

| Chỉ số | Tuần này (06–12/09) | Đổi so với 30/08–05/09 |
|---|---|---|
| Người dùng hoạt động | 209 | ↑ +8.3% |
| Phiên | 312 | ↓ **−5.7%** |
| Lượt xem trang | ~1,181 | ↓ **−4.5%** |
| Key events | 74 | ↑ +311.1% |

**Đối chiếu độc lập xác nhận Sessions đúng là giảm:** 312 (tuần này) so với
331 (tuần trước, đọc từ Traffic acquisition VÀ Landing page report — hai
nguồn độc lập đều ra 331, đáng tin) → 312/331 − 1 = **−5.74%**, khớp khít với
mũi tên đỏ của Home. Views: 1,181 (tuần này) vs 1,237 (tuần trước, đọc từ
Pages and screens) → −4.53%, cũng khớp.

**Đã tự kiểm thêm hai tỷ lệ phái sinh và SỬA LẠI cho đúng chiều (bản nháp
trước đảo ngược cả hai):** phiên/người-dùng-hoạt-động giảm từ 1.72 (331/193)
xuống 1.49 (312/209) — mỗi người active tham gia ÍT phiên hơn trung bình,
không phải nhiều hơn. Lượt xem/phiên gần như KHÔNG ĐỔI: 3.74 (1237/331) →
3.79 (1181/312), tức nhích lên ~1.3%, không giảm. Đọc đúng: người dùng hoạt
động tăng (209 vs 193) nhưng mỗi người quay lại ít phiên hơn; mỗi phiên vẫn
xem sâu như cũ. Điều chỉnh quan trọng so với bản nháp đầu: **tổng phiên và
tổng lượt xem đều giảm thật**, chỉ có key events và active users tăng.

**Key events +311% không phải tăng trưởng thật — là hiệu ứng đếm.**
`apply_clicked` và `newsletter_subscribed` chỉ được đánh dấu key event từ
02/09/2026, nên tuần 30/08–05/09 chỉ tính được phần key event xảy ra SAU
02/09 (18 sự kiện). Tuần này là tuần ĐẦU TIÊN trọn vẹn nằm sau mốc đó:
59 `apply_clicked` + 15 `newsletter_subscribed` = 74, khớp chính xác con số
Home report đưa ra. Từ tuần sau, % này mới so được táo với táo.

**`newsletter_subscribed`: 2 (tuần trước, đã settle) → 15 (tuần này, CHƯA
settle — 11–12/09 còn trong cửa sổ 24–48h)** — không bị hiệu ứng đếm của mốc
02/09 (event này đăng ký từ trước), nhưng con số 15 vẫn là số QUAN SÁT ĐƯỢC,
chưa phải số cuối cùng. Tăng đáng chú ý dù có điều chỉnh nhẹ ở lượt đọc sau —
đáng theo dõi nguồn nào kéo (xem mục 2, 4).

## 2. Kênh và nguồn

| Kênh | Phiên tuần này | Phiên tuần trước (settled) | Đổi |
|---|---|---|---|
| Organic Social | 170 | 108 | +57% |
| Direct | 72 | 129 | **−44%** |
| Unassigned | 62 | 7 | +786% |
| Organic Search | 25 | 84 | **−70%** |
| Referral | 0 | 3 | −100% |

**Bảng này VẪN KHÔNG cộng khớp tổng**, như tuần trước: 170+72+62+25 = 329,
còn Total báo 312 — thừa 17 phiên (5.4%). Nhẹ hơn nhiều so với 47/323 (14.5%)
của tuần trước khi đọc dữ liệu tươi, khớp với giả thuyết "dữ liệu 1–2 ngày
cuối chưa ổn định": 12/09 (hôm qua) là ngày trẻ nhất trong cửa sổ này.

**Organic Search giảm 84 → 25 và Direct giảm 129 → 72 là giảm TUYỆT ĐỐI —
và (đã sửa ở mục 1) tổng phiên cả tuần cũng giảm 5.7%, không đứng yên như bản
nháp đầu viết.** Organic Social tăng (108 → 170) không bù được phần giảm của
hai kênh kia, nên đây không phải "dịch chuyển cơ cấu trong lúc tổng không
đổi" — **là giảm thật ở Search và Direct, giảm thật ở tổng, còn Social và
Unassigned là hai kênh đi ngược hướng (đều tăng)**. Riêng Unassigned tăng
mạnh (7 → 62, xem cảnh báo thừa dòng ở trên) làm bức tranh khó đọc thêm: một
phần của 62 phiên đó CÓ THỂ vốn thuộc
Organic Search hay Direct nhưng bị phân loại sai lúc dữ liệu còn tươi — nếu
đúng vậy, phần giảm thật của hai kênh này nhỏ hơn số đang thấy. **Chưa có
cách tách được bao nhiêu phần trăm của Unassigned "đáng lẽ" thuộc kênh nào**
— cần nhìn thêm 1–2 tuần, và đọc lại đúng 30/08–05/09 lẫn 06–12/09 sau khi cả
hai đã settle hoàn toàn để loại nhiễu.

Nguồn/medium (Session source/medium), tuần này:

| Nguồn | Phiên |
|---|---|
| `facebook.com/referral` | 113 |
| `(direct)/(none)` | 72 |
| `catch-the-points/game` | 39 |
| `l.facebook.com/referral` | 34 |
| `google/organic` | 24 |
| `(not set)` | 21 |
| `lm.facebook.com/referral` | 16 |
| `m.facebook.com/referral` | 7 |
| `hero/` (medium rỗng) | 2 |
| `bing/organic` | 1 |

Tổng Facebook (facebook.com + l.facebook.com + lm.facebook.com +
m.facebook.com) = **170 phiên**, khớp đúng con số Organic Social ở bảng kênh.

**Hai điều CHƯA GIẢI THÍCH ĐƯỢC, cả hai đã kiểm code, không phải suy diễn:**

1. **`catch-the-points/game` vẫn có 39 phiên tuần này**, dù UTM
   `utm_source=catch-the-points&utm_medium=game` đã bị GỠ khỏi code
   (`grep` toàn repo không còn chỗ nào phát ra UTM này — chỉ còn một comment
   ở `recommendations.js:24` mô tả "bản trước", không phải code đang chạy).
   **Sửa lại mốc thời gian ở đây:** commit gỡ UTM (`a8cbd48`) không phải
   "trước 06/09" như bản nháp ghi — `git show -s --format=%ci a8cbd48` trả
   **`2026-09-06 13:55:49 -0400`**, tức TRƯA 06/09, ngay trong cửa sổ dữ liệu
   đang xét. Điều này làm giả thuyết "toàn bộ 39 phiên là link cũ đã lưu từ
   trước tuần này" YẾU ĐI: một phần có thể chỉ đơn giản là người bấm link
   được sinh ra sáng 06/09 (trước ~14h), khi code CŨ (còn UTM) vẫn đang chạy
   trên site — không cần giả thuyết "bookmark/chia sẻ cũ". Không có cách tách
   39 phiên theo giờ trong báo cáo này để phân biệt hai khả năng, nên **cả
   hai đều chỉ là giả thuyết, chưa cái nào được xác nhận.**
2. **`hero/` — nguồn lạ, 2 phiên, medium RỖNG.** Đã kiểm một khả năng và LOẠI
   nó ở mức lý thuyết chuẩn: `src/components/home/hero.tsx` truyền
   `source="hero"` vào `NewsletterForm`, nhưng đó là tham số của EVENT
   `newsletter_subscribed` (custom dimension "Newsletter source"), không phải
   `utm_source` — GA4 không suy ra Session source/medium từ một tham số event
   tự đặt tên tuỳ ý. `grep -rn "utm_source"` toàn repo không có chỗ nào gán
   `hero`.

   **Nhưng có một sự trùng hợp đáng ngờ chưa loại trừ hết:** đọc thêm dữ liệu
   `newsletter_subscribed` theo "Newsletter source" tuần này, **15/15 lượt
   đăng ký đều mang `source=hero`** — tức 100% tới từ cùng một chữ "hero".
   Site này từng có đúng một lần một tham số event tự đặt tên trùng với một
   khái niệm GA4 dành riêng (`tracking_id`, xem AGENTS.md 06/09) khiến hit bay
   sai chỗ. **Đã kiểm và LOẠI TRỪ khả năng này ngay trong lượt làm báo cáo**
   (đúng phương pháp AGENTS.md đã lập cho vụ `tracking_id`: gọi thẳng
   `window.gtag('event', 'newsletter_subscribed', { source:
   'verify_hero_param_20260913' })` trên ghe1a.com thật rồi đọc request
   `g/collect`) — kết quả `tid=G-5EJS75L7SK` (ĐÚNG measurement ID) và
   `ep.source=verify_hero_param_20260913` (tham số đi tới đúng chỗ). **"source"
   KHÔNG phải tên dành riêng của gtag** — giả thuyết bị bác. Nguồn gốc thật
   của 2 phiên `hero/` trong Session source/medium **vẫn chưa tìm ra**; đây
   chỉ là một khả năng đã loại, không phải lời giải.
   *(Đã gửi 2 event thử `source=verify_hero_param_20260913` vào property
   thật lúc kiểm — giá trị nhận dạng được, không lẫn với dữ liệu người đọc
   thật, ngày 13/09/2026. **Lưu ý cho lượt đọc tuần sau:** 2 event này VẪN
   cộng vào tổng `newsletter_subscribed` / key events của tuần chứa ngày
   13/09 trong GA4 — khi đọc báo cáo tuần tới, trừ 2 khỏi tổng
   `newsletter_subscribed` nếu 13/09 rơi vào cửa sổ đó, đừng để lẫn vào số
   người đăng ký thật.)*

## 3. Mini-game: giảm mạnh sau tuần ra mắt (chưa rõ vì sao)

| Sự kiện | Tuần ra mắt (30/08–05/09, chủ yếu 05/09) | Tuần này |
|---|---|---|
| `game_started` | 144 | 36 (−75%) |
| `game_completed` | 121 | 30 (−75%) |
| `rank_achieved` | 130 | 30 (−77%) |
| `personal_best` | 63 | 7 (−89%) |
| `play_again` | 15 | 11 |
| `game_over_balance` | 9 | 0 |

Giảm mạnh KHỚP với dự đoán của báo cáo trước ("một ngày ra mắt được Facebook
đẩy không chứng minh được điều đó [game hiệu quả hơn blog] lâu dài"), nhưng
**"hết novelty" chỉ là MỘT giả thuyết, không phải kết luận** — dữ liệu này
cũng khớp đều với khả năng khác là site đơn giản giảm quảng bá game sau tuần
ra mắt (ít link game hơn trên Facebook, không phải người chơi tự nhiên chán).
Không phân biệt được hai khả năng từ số liệu đang có. `post_game_recommendation_shown`/`_clicked`
đã chạy được (mục 0), nhưng mẫu quá nhỏ (12 lượt hiện, 1 lượt bấm) để nói
được gì về tỷ lệ chuyển đổi từ game sang thẻ.

## 4. `apply_clicked` theo trang, theo thẻ, theo vị trí — KHÔNG PHẢI doanh thu

**Sửa cách gọi so với báo cáo trước: `apply_clicked` là click ra ngoài
(outbound), không phải doanh thu đã xác nhận.** Event bắn ngay lúc bấm, không
biết application có hoàn tất hay hoa hồng có phát sinh không. `ApplyButton`
còn đo cả link ngân hàng KHÔNG phải affiliate (`affiliate={isReferralUrl(...)}`
— xem `src/components/ui/apply-button.tsx`), nên tổng 59 không phải "59 lượt
ra tiền". Đọc đúng: "59 lượt bấm apply ra ngoài", không hơn.

| Kỳ | `apply_clicked` (sự kiện / người) |
|---|---|
| 30/08–05/09 (settled) | 56 / 33 |
| 06/09–12/09 | **59 / 28** |

Sự kiện gần như đứng yên (+5%) nhưng **người bấm giảm 15%** (33 → 28) — nghĩa
là tuần này ít người bấm hơn nhưng mỗi người bấm nhiều lần hơn trung bình
(1.70 → 2.11 sự kiện/người). Chưa đủ dữ liệu để nói đây là xấu hay tốt (có
thể là người đang so sánh nhiều thẻ trước khi chọn).

### 4.1 Theo trang (`page_location`, không cần custom dimension)

| Trang | Click tuần này | Click tuần trước |
|---|---|---|
| `/credit-cards` | **21 (36%)** | 19 (34%) |
| `/credit-cards/tot-nhat/offers` | 9 (15%) | — (trang mới) |
| `/` (trang chủ) | 7 (12%) | 0 |
| `/credit-cards/scotiabank-gold-amex` | 4 | 2 |
| `/credit-cards/amex-aeroplan-reserve` | 4 | 2 |
| `/credit-cards/rbc-avion-visa-infinite` | 2 | 0 |
| `/credit-cards/td-first-class-travel-visa-infinite` | 1 | 3 |
| `/credit-cards/so-sanh` | **0** | 4 |
| 8 trang khác (5 trang thẻ 1 click/trang, 3 bài blog, `/bank-accounts` 2 click) | 11 | — |

*(Sửa lỗi bảng ở bản trước: dòng gộp cuối ghi nhầm "9 trang thẻ lẻ khác,
1 mỗi trang" — cộng cả bảng chỉ ra 57/59, thiếu 2. Dòng gộp thực ra gồm 8
trang khác nhau, không phải toàn "thẻ": 5 trang thẻ (1 click/trang), 3 bài
blog (2+1+1 click), và `/bank-accounts` (2 click) — tổng đúng 11, cộng cả
bảng ra đủ 59.)*

**`/credit-cards/so-sanh` rơi từ 4 click xuống 0 tuần này**, dù trang vẫn
được 26 lượt xem. Báo cáo trước gọi tỷ lệ 4/12 là "33% CTR cao nhất site" —
**cách gọi đó phóng đại**: mẫu 12 lượt xem quá nhỏ để tính CTR đáng tin, và
tỷ lệ event/pageview không phải CTR theo người dùng (một người có thể bấm
nhiều lần, xem mục double-count dưới). Đề xuất tuần trước ("đưa người vào đây
nhiều hơn") chưa được thực hiện. **Không đủ căn cứ kết luận trang này hết
hiệu quả HAY từng hiệu quả — chỉ đủ căn cứ nói cỡ mẫu quá nhỏ để tin bất kỳ %
nào từ trang này ở cả hai tuần.**

`/credit-cards/tot-nhat/offers` (9 click, 15%) là trang mới xuất hiện trong
bảng — một trong bốn trang "Các thẻ tốt nhất". Đáng theo dõi tiếp.

### 4.2 Theo thẻ/sản phẩm (`Product`, custom dimension — TUẦN ĐẦU TIÊN có dữ liệu)

| Thẻ/sản phẩm | Click |
|---|---|
| RBC® Avion® Visa Infinite | 9 |
| CIBC® Aventura® Gold Visa | 6 |
| Scotiabank® Gold American Express® | 6 |
| Amex Aeroplan®* Reserve | 5 |
| Amex Marriott Bonvoy® | 5 |
| RBC® Avion® Visa Platinum | 4 |
| TD First Class Travel® Visa Infinite* | 4 |
| Amex Aeroplan®* Business Reserve | 3 |
| 11 sản phẩm khác | 1–2 mỗi thẻ |

**Cảnh báo cách đọc cột "Product" — đã kiểm code, không phải phỏng đoán:**
3 dòng trong 22 dòng gốc của báo cáo này KHÔNG phải tên thẻ, mà là SLUG BÀI
BLOG (`3-thu-khong-the-thieu-khi-du-lich-nuoc-ngoai`,
`canadian-credit-card-points-101-nen-tich-diem-nao`,
`tai-sao-nen-co-nhieu-point-currencies`). Đã đọc
[`affiliate-click-tracker.tsx`](../src/components/blog/affiliate-click-tracker.tsx)
và [`blog/[slug]/page.tsx:264`](../src/app/blog/%5Bslug%5D/page.tsx): với
click từ THÂN BÀI (`placement: "post_body"`), tham số `product` được gán
`post.slug` — **slug của BÀI VIẾT đang chứa link, không phải slug của
THẺ được link tới**. Bảng trên đã LOẠI 3 dòng slug-bài-viết này để không
lẫn "thẻ nào" với "bài nào".

**Phát hiện thêm khi Codex chất vấn câu trên (đã tự đọc code xác nhận, KHÔNG
chỉ tin lời Codex) — RỦI RO ĐẾM ĐÔI CÓ THẬT TRONG CODE, dù tuần này chưa xảy
ra (xem phép tính bên dưới):** thân bài không chỉ có link chữ trong prose, mà
còn có khối `CardSpotlight` (chèn giữa đoạn văn khi bài nhắc một thẻ cụ thể —
xem `post-body.tsx`), gồm CẢ nút Apply (`ApplyButton`) LẪN ảnh thẻ
(`CardImage`) — cả hai đều dùng `ApplyLink` bên dưới. Khối này nằm BÊN TRONG
`data-affiliate-scope="post-body"`, và `post-body.tsx` truyền
`placement="post_body"` xuống `CardSpotlight`. Nút Apply giữ nguyên
`placement="post_body"`; ảnh thẻ (`CardImage`) tự thêm hậu tố
(`` placement={`${placement}_image`} `` — xem `card-image.tsx`), thành
`placement="post_body_image"`. Cả hai anchor đều CŨNG khớp selector
`a[rel~='sponsored']` của `AffiliateClickTracker` — listener gắn ở capture
phase trên chính `data-affiliate-scope`. Một click vào nút Apply HOẶC ảnh
của khối thẻ nhúng trong bài vì vậy bắn **HAI** event `apply_clicked` cùng
lúc: một mang `product=card.slug` (từ `ApplyLink`, placement là
`post_body` hoặc `post_body_image` tuỳ bấm nút hay ảnh), một mang
`product=post.slug` (từ `AffiliateClickTracker`, LUÔN mang
`placement="post_body"` bất kể bấm vào đâu trong khối, vì nó không phân biệt
được nút với ảnh). Comment trong `post-body.tsx` viết "Nút Apply trong khối
thẻ tự bắn event riêng của nó… nên không bị đếm hai lần" — **comment đó SAI
theo cách đọc code hiện tại**, trừ khi có cơ chế chặn propagation nào khác
chưa tìm ra.

**Tuần này, hai phép kiểm riêng cho thấy lỗi trên CHƯA XẢY RA lần nào (N=0),
dù bug vẫn có thật — đã sửa lại cách tính sau khi Codex chỉ ra một chi tiết
mình bỏ sót:** `CardImage` (dùng cho ẢNH thẻ trong `CardSpotlight`) tự thêm
hậu tố vào placement nó nhận (`` `${placement}_image` `` — xem
`card-image.tsx`), nên click vào ẢNH sẽ mang `placement="post_body_image"`,
KHÁC với `placement="post_body"` của nút Apply. Vậy phải kiểm hai đường riêng:

1. **Đường nút Apply:** nếu double-fire xảy ra N lần, `placement=post_body`
   sẽ có N event mang tên thẻ CỘNG THÊM số event mang tên bài viết; nhưng
   tổng `post_body` tuần này là **4** (mục 4.3), và tổng 3 dòng "Product"
   dạng slug-bài-viết ở trên cũng cộng đúng **4** (2+1+1) — không còn dư
   event nào để là tên thẻ. N=0 cho đường này.
2. **Đường ảnh thẻ:** bảng Placement ở mục 4.3 liệt kê ĐỦ 10 dòng, cộng đúng
   59 — và không có dòng `post_body_image` nào. N=0 cho đường này (không ai
   bấm ảnh thẻ trong khối nhúng tuần này).

Cả hai đường đều N=0 tuần này. **Nhưng đây là may mắn của tuần này, không
phải bằng chứng bug đã hết** — tuần nào có người bấm nút Apply HOẶC ảnh của
khối `CardSpotlight` nhúng trong bài, `post_body`/`post_body_image` và bảng
Product ở mục 4.2 sẽ bị thổi phồng, và hai phép kiểm ở trên là cách phát hiện
cho các tuần sau. Sửa lỗi (chặn double-fire, hoặc bỏ `CardSpotlight` khỏi
scope của tracker) là việc code, **chưa làm trong lượt này** — ghi lại cho
phiên khác.

### 4.3 Theo vị trí (`Placement`)

| Placement | Click |
|---|---|
| `card_list` (danh sách `/credit-cards`) | 18 (31%) |
| `card_detail_rail` | 9 (15%) |
| `card_detail` | 7 (12%) |
| `best_cards_offers` | 6 (10%) |
| `home_offers_image` | 6 (10%) |
| `post_body` (link trong thân bài) | 4 (7%) |
| `best_cards_offers_image` | 3 (5%) |
| `card_list_image` | 3 (5%) |
| `account_list` | 2 (3%) |
| `home_offers` | 1 (2%) |

`card_list` một mình chiếm gần 1/3 tổng click — khớp với mục 4.1
(`/credit-cards` có nhiều click apply nhất, không phải "ra tiền nhất" — xem
cảnh báo đầu mục 4). Vùng ẢNH thẻ (`*_image`, ba dòng) cộng lại 12 click —
xác nhận quyết định đo `CardImage` như một đường click riêng (AGENTS.md
30/08) là đúng, không phải chi tiết thừa.

## 5. Search Console — CHƯA ĐỦ DỮ LIỆU để đọc tuần này

| | 06/09–12/09 (RAW, CHƯA SETTLE) | 30/08–05/09 (settled) |
|---|---|---|
| Click | 5 | 24 |
| Impression | 13 | 50 |

**Đừng đọc con số cột trái như tăng trưởng âm — dữ liệu cửa sổ này gần như
chắc chắn CHƯA ĐẦY ĐỦ, không so được với tuần trước.** Search Console có độ
trễ hiển thị (Google tài liệu hoá là "có thể còn preliminary trong vài ngày
gần nhất", không cam kết một con số cố định — sửa lại cách viết "trễ đúng
2 ngày" của bản nháp trước, đó là con số ước lượng thường gặp, không phải quy
tắc cứng) và dùng múi giờ Pacific, nên cửa sổ 06–12/09 nhiều khả năng thiếu
một phần dữ liệu của 11/09 và 12/09. **Không có cách chứng minh bằng phép
toán rằng phần thiếu đó "đủ giải thích" mức chênh lệch quan sát được** — đây
chỉ là lý do hợp lý nhất cho việc số liệu thấp bất thường, không phải một
phép tính đã kiểm chứng. Kết luận đúng mức: **5 click / 13 impression của
tuần này là số CHƯA ĐẦY ĐỦ, chưa dùng được để so sánh hay kết luận gì** —
không phải "tăng trưởng âm 79%".

Số liệu tuần trước, khi đọc lại lúc đã settle, tự nó cũng tăng nhẹ so với bản
báo cáo cũ (23→24 click, 48→50 impression) — xác nhận GSC vẫn còn "nhích" số
vài ngày sau khi hiển thị lần đầu.

**23/24 click tuần trước (settled) vẫn từ query thương hiệu `ghe1a`.** Kết
luận "vấn đề là thứ hạng chứ không phải hiển thị" (từ báo cáo 06/09) dựa trên
việc các query phi thương hiệu CÓ impression, ở vị trí 30–77 — tức các query
CỤ THỂ đó có hiển thị, chỉ xếp hạng thấp. **Điều đó chưa chứng minh được kết
luận tổng quát hơn "nút thắt CHUNG của toàn site là thứ hạng"** — cần nhìn
tổng impression/vị trí trung bình của TOÀN BỘ query phi thương hiệu cộng lại,
chưa làm ở báo cáo nào tới nay. Giữ kết luận hẹp (đúng cho các query đã liệt
kê), không mở rộng thành kết luận toàn site. Đọc lại toàn bộ sau 15/09 khi
tuần này đã đầy đủ.

## 6. Địa lý và YouTube

Active users theo quốc gia (7 ngày): Canada 197 (+17%), Vietnam 8 (+700%,
nền quá nhỏ để đọc thành xu hướng — 1 → 8 người), Mỹ 2, Trung Quốc 1,
Ireland 1, Hàn Quốc 1.

**YouTube vẫn đúng 0 phiên** — không có dòng `youtube` nào trong 10 dòng đầy
đủ của bảng Session source/medium tuần này. Đúng dự đoán:
[`feedback_ghe_1a_youtube_valve_order`](../../../../.claude/projects/-Users-hoangle-Developer-Claude-Code/memory/feedback_ghe_1a_youtube_valve_order.md)
— van chỉ nên mở sau khi có bài thật đứng sau video, chưa có gì thay đổi ở
đây để mở van.

## 7. Điều nên làm, xếp theo giá trị

1. **Không cần hành động gì cho mục 0** — cả hai việc sửa tuần trước đã tự
   chứng minh bằng dữ liệu thật, không cần theo dõi thêm.
2. **`/credit-cards/so-sanh` giảm còn 0 click — theo dõi thêm 1 tuần trước
   khi kết luận.** Đề xuất tuần trước ("đưa người vào đây nhiều hơn") chưa
   được thực hiện; nếu tuần sau vẫn 0 thì mới đáng lo, còn giờ là N quá nhỏ.
3. **Đọc lại Search Console sau 15/09/2026** — số hiện tại (mục 5) gần như
   chắc chắn chưa đủ, đừng trích dẫn ở đâu khác.
4. **`newsletter_subscribed` 2 → 15, và cả 15 (Event count = Total users = 15
   trong GA4, tức 15 người dùng riêng biệt) đều mang `source=hero`.** Đã đọc
   bằng custom dimension "Newsletter source" (đăng ký từ 06/09) — không có
   lượt nào từ `page_cta` hay `start_here`, dù cả ba cửa đều đang hoạt động.
   **Chỉ dừng ở đây, không suy thêm về nguyên nhân:** dữ liệu nói ĐƯỢC "cả 15
   lượt đăng ký tuần này qua form ở hero", nhưng KHÔNG nói được "hero hiệu
   quả hơn" hay "hai cửa kia có vấn đề" — tuần trước (2 lượt) không có
   breakdown theo nguồn để so sánh, nên chưa có đường nào để kết luận nguyên
   nhân tăng. Việc cho tuần sau: tiếp tục đọc theo `source` để có đủ dữ liệu
   so sánh.
5. **Theo dõi `catch-the-points/game` và `hero/` ở tuần sau — nhưng đừng
   coi xu hướng của nó là phép kiểm chứng.** Sửa lại logic của bản nháp
   trước: dù `catch-the-points/game` giảm về 0, đứng yên, hay tăng, đều
   KHÔNG phân biệt được "link cũ còn trôi nổi" với "một nguồn khác chưa tìm
   ra vẫn đang phát UTM này" — cả hai giả thuyết đều tương thích với mọi
   hướng diễn biến. Muốn phân biệt thật thì phải tách 39 phiên theo GIỜ
   trong ngày 06/09 (so với giờ deploy commit `a8cbd48`, 13:55 Toronto) hoặc
   tìm được nơi phát UTM này bằng cách khác (log server, hoặc hỏi xem có
   trang ngoài site nào từng nhúng link này không) — không có trong phạm vi
   báo cáo tuần.
6. **YouTube: giữ nguyên, KHÔNG mở van tuần này.** Điều kiện mở van (có bài
   thật đứng sau video — [feedback_ghe_1a_youtube_valve_order](../../../../.claude/projects/-Users-hoangle-Developer-Claude-Code/memory/feedback_ghe_1a_youtube_valve_order.md))
   vẫn chưa đổi so với tuần trước. Đây là dòng ghi nhận trạng thái, không phải
   việc cần làm tuần này.

## 8. Chắc chắn / chưa đủ căn cứ / còn treo

**CHẮC CHẮN** (đọc trực tiếp từ GA4, hoặc kiểm bằng code/network request —
không suy luận thêm):
- `apply_clicked` 59 sự kiện / 28 người, `newsletter_subscribed` 15 — tuần
  này (số quan sát được, 11–12/09 chưa settle — xem mục 1).
- Cách đọc "Unassigned tuần trước là dữ liệu chưa xử lý xong" đã đúng — xác
  nhận bằng dữ liệu ĐÃ SETTLE (30/08–05/09). Việc sửa `tracking_id` cũng
  đúng, nhưng bằng chứng của nó (`post_game_recommendation_shown`=12 tuần
  này) đến từ dữ liệu CHƯA settle hoàn toàn — vẫn đáng tin vì đây là bước
  nhảy từ 0 tuyệt đối lên khác 0, loại thay đổi mà việc settle (thường chỉ
  chỉnh vài phần trăm) không tự tạo ra hay xoá đi được.
- YouTube vẫn 0 phiên.
- Cả 15 `newsletter_subscribed` tuần này đều mang `source=hero`, 0 từ
  `page_cta`/`start_here`.
- `AffiliateClickTracker` gán `product` = slug bài viết cho click TRONG THÂN
  BÀI — đọc từ code, không phải suy luận từ dữ liệu GA4.
- **Lỗi đo mới phát hiện: click vào nút Apply của `CardSpotlight` nhúng trong
  blog post bắn HAI event `apply_clicked` (một `product`=thẻ, một
  `product`=bài viết) — xác nhận bằng đọc `post-body.tsx`,
  `card-spotlight.tsx`, `apply-link.tsx` và `affiliate-click-tracker.tsx`.
  Comment trong code hiện tại nói ngược lại (khẳng định không đếm đôi) —
  comment đó sai, cần sửa ở phiên code riêng.**
- Tham số event `source` KHÔNG phải tên dành riêng của gtag — xác nhận bằng
  test trực tiếp trên production (`g/collect` trả đúng `tid`).

**CHƯA ĐỦ CĂN CỨ:**
- Organic Search giảm 70% và Direct giảm 44% có phải xu hướng thật hay chỉ là
  nhiễu/trùng hợp cùng lúc Facebook tăng — cần thêm 1–2 tuần, KHÔNG được quy
  nguyên nhân cho Facebook như bản nháp đầu từng viết.
- `/credit-cards/so-sanh` mất hết click có phải suy yếu thật — N quá nhỏ ở cả
  hai tuần, "33% CTR" của tuần trước là cách gọi phóng đại cần rút lại.
- Vì sao `newsletter_subscribed` chỉ đến từ `source=hero` — chưa đủ dữ liệu
  để nói đây là hero mạnh hay hai cửa kia có vấn đề.
- Mức ảnh hưởng của lỗi đếm đôi ở CÁC TUẦN SAU — tuần này đã tính được là
  N=0 (xem phép tính ở mục 4.2), nhưng chưa có cách tự động phát hiện N cho
  các tuần tới ngoài việc lặp lại đúng phép so sánh đó mỗi lần.

**CÒN TREO, CHƯA NGÃ NGŨ (kể cả sau review Codex — xem mục 9):**
- Nguồn gốc thật của 39 phiên `catch-the-points/game` — mốc thời gian
  `a8cbd48` (trưa 06/09) làm yếu giả thuyết "toàn bộ là link cũ đã lưu", chưa
  có giả thuyết nào thay thế được xác nhận.
- Nguồn gốc của 2 phiên `hero/` (medium rỗng) trong Session source/medium —
  đã loại một khả năng (`source` không phải tên dành riêng của gtag), vẫn
  chưa tìm ra nguyên nhân thật.
- Vì sao bảng phiên vẫn thừa 17/312 dù đã bớt hẳn so với tuần trước (47/323).

## 9. Phản biện Codex (`gpt-5.6-sol`, effort high, 13/09/2026)

**Vòng 1** (chất vấn bản nháp đầu, dựa trên số liệu coi là đúng, chỉ soi suy
luận và khẳng định về code): 7 phát hiện, tự kiểm lại từng cái trước khi sửa.

- **Đồng ý và đã sửa:** lỗi đếm đôi `apply_clicked`/`CardSpotlight` trong
  thân bài (mục 4.2, đã tự đọc `post-body.tsx`/`card-spotlight.tsx`/
  `apply-link.tsx`/`affiliate-click-tracker.tsx` xác nhận đúng); ngày commit
  `a8cbd48` sai (đã tự chạy `git show` xác nhận: trưa 06/09, không phải
  "trước 06/09"); suy luận "Facebook kéo % kênh khác xuống" vượt bằng chứng;
  toán Search Console "thiếu 2 ngày đủ giải thích" không chứng minh được;
  gọi `apply_clicked` là "doanh thu" sai thuật ngữ; gọi `newsletter_subscribed`
  tuần này là "settled" khi 11–12/09 chưa qua cửa sổ 24–48h; câu "CỘNG ĐÚNG"
  tự mâu thuẫn với phép cộng ngay sau nó.
- **Đồng ý một phần:** nguồn `hero` — Codex đúng là code CÓ phát ra chữ
  "hero" (tham số `source` của `NewsletterForm`), nhưng đó không tự động
  chứng minh nó là nguồn gốc của Session source `hero/`. Đã tự kiểm thêm một
  bước Codex không yêu cầu: gọi `window.gtag()` thật trên production để xác
  nhận `source` không phải tên dành riêng của gtag — bác được MỘT khả năng,
  không giải được bí ẩn.

**Vòng 2** (bắt Codex bác lại chính bản vá vòng 1): phát hiện thêm 1 lỗi
NGHIÊM TRỌNG mà bản thân việc sửa vòng 1 không bắt được — **Sessions và Views
trong bảng số tổng bị ghi NHẦM DẤU thành tăng, trong khi Home report thật
hiện mũi tên ĐỎ (giảm)**. Đã tự chụp lại màn hình gốc (không chỉ đọc text) để
xác nhận, và đối chiếu độc lập bằng tổng phiên từ hai báo cáo khác (Traffic
acquisition, Landing page) — cả hai đều xác nhận 312 < 331, tức giảm thật.
Đây là lỗi ĐỌC SỐ, không phải lỗi suy luận, và nó làm sai cả khung diễn giải
của mục 2 (đã viết lại: không còn "dịch chuyển cơ cấu trong lúc tổng không
đổi", mà là "Search và Direct giảm thật, tổng phiên cũng giảm thật").

Các phát hiện khác của vòng 2 đã sửa: tiêu đề mục 3 "đã hết novelty" (đổi
thành trung tính); logic sai ở việc-nên-làm mục 5 (xu hướng của
`catch-the-points/game` không phân biệt được hai giả thuyết theo bất kỳ
hướng nào); độ trễ Search Console "đúng 2 ngày" (Google không cam kết con số
cố định); kết luận "vấn đề thứ hạng" bị mở rộng quá phạm vi dữ liệu đã có;
`CardImage` cũng nằm trong rủi ro đếm đôi, không chỉ nút Apply; cảnh báo dữ
liệu thử `verify_hero_param_20260913` sẽ lẫn vào tổng tuần chứa 13/09.

**Đã BÁC một phần phát hiện của Codex (tự kiểm rồi giữ nguyên, không sửa):**
Codex nói "15 người bấm đăng ký" là "vượt bằng chứng, GA4 chỉ cho biết 15
event chứ không phải 15 người dùng riêng biệt". **Sai — đã kiểm lại bảng gốc:
GA4 trả rõ cả cột "Event count"=15 VÀ "Total users"=15 (tỷ lệ 1.00 sự
kiện/người)**, tức đúng là 15 người dùng riêng biệt, không phải suy luận từ
số event. Giữ nguyên "15 người" trong báo cáo, chỉ bỏ phần suy diễn nguyên
nhân ("hero làm việc tốt hơn hai cửa kia") vì phần đó thật sự vượt bằng
chứng.

**Câu hỏi chốt của vòng 2 ("báo cáo còn chặn được không"): Codex trả lời
"vẫn chặn" ở bản trước khi sửa — sau khi áp toàn bộ sửa chữa ở trên (đặc
biệt là lỗi dấu Sessions/Views), báo cáo này đã ở trạng thái đủ trung thực
về mức độ chắc chắn để giao cho người ra quyết định**, với điều kiện đọc
đúng ba nhãn CHẮC CHẮN / CHƯA ĐỦ CĂN CỨ / CÒN TREO ở mục 8 thay vì chỉ đọc
số liệu bề mặt.

**Vòng 3** (kiểm hẹp, chỉ soi hai chỗ tự mình sửa thêm SAU vòng 2 — lỗi dấu
Sessions/Views và phép tính N=0): bắt thêm 4 lỗi thật, đã tự kiểm và sửa cả
4 — (1) hai tỷ lệ phái sinh phiên/người và lượt-xem/phiên bị viết NGƯỢC
chiều (đã tự pull thêm số Views tuần trước = 1,237 để tính lại cho đúng);
(2) bảng 4.1 chỉ cộng ra 57/59 do dòng gộp cuối gọi sai tên và sai số (sửa
thành đúng 8 trang/11 click, có cả blog và ngân hàng, không chỉ "thẻ"); (3)
`CardImage` tự thêm hậu tố `_image` vào placement nó nhận, nên phép tính N=0
cho đường ẢNH cần kiểm riêng (bảng Placement không có dòng `post_body_image`
— N=0 cho đường này) thay vì gộp chung với đường NÚT như bản trước; (4) vài
chỗ câu chữ tự mâu thuẫn (tiêu đề "đã hết novelty", "chỉ riêng Social đi
ngược hướng" trong khi Unassigned cũng tăng, gọi bằng chứng `tracking_id`
là "settled" khi nó đến từ dữ liệu tuần chưa settle). Không có phát hiện nào
của vòng 3 cần bác lại.
